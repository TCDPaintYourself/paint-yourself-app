const { create, globSource } = require('ipfs-http-client')

const client = create()

/*
TODO
1. Upload individual image
2. Add hash to metadata json
3. Upload folder containing one file named "1"
4. Return folder ipfs hash
*/

export const uploadTokenImage = () => {}
export const uploadMetadataFolder = () => {}
